<%
var sets = array(data.set);
if (sets[0].classificationSummary){
	var accuracy = new Array();
	for(var i=0; i < sets.length; i++)
		accuracy.push((100*sets[i].classificationSummary.accuracy).toPrecision(4)+"%");
		
	%>Accuracy: <%=accuracy.join(" / ") %>
<%}else{
	var rmses = new Array();
	for(var i=0; i < sets.length; i++)
		rmses.push((1.0*sets[i].rmse).toFixed(3));
		%>RMSE: <%=rmses.join(" / ") %>
<%}%>
<br/>